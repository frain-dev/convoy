#!/usr/bin/env bash

#MISE description="Build Convoy binary"
#MISE dir="{{ config_root }}"
#MISE sources=["go.mod", "go.sum", "**/*.go", "VERSION"]
#MISE depends=["dashboard:build"]
#MISE outputs=["bin/convoy"]

set -e

echo "🚧 Building Convoy..."

APP_VERSION=$(cat ./VERSION)
go build -ldflags="-X main.release=${APP_VERSION}" -o bin/convoy ./cmd/*.go
