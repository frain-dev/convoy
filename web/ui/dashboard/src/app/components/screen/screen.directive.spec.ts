import { ScreenDirective } from './screen.directive';

describe('ScreenDirective', () => {
  it('should create an instance', () => {
    const directive = new ScreenDirective();
    expect(directive).toBeTruthy();
  });
});
