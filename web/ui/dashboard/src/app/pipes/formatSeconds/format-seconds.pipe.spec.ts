import { FormatSecondsPipe } from './format-seconds.pipe';

describe('FormatSecondsPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatSecondsPipe();
    expect(pipe).toBeTruthy();
  });
});
