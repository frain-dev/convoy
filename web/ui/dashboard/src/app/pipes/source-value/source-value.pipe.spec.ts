import { SourceValuePipe } from './source-value.pipe';

describe('SourceValuePipe', () => {
  it('create an instance', () => {
    const pipe = new SourceValuePipe();
    expect(pipe).toBeTruthy();
  });
});
